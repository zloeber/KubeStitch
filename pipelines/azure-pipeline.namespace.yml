# Pipeline Overview
name: $(BuildDefinitionName).$(DayOfYear)$(Rev:.r)
trigger:
  batch: true
  paths:
    include:
      - 'charts/namespace'

resources:
  repositories:
    - repository: platform
      type: git
      name: Pipelines

stages:
- template: publish/helm-chart.yml@platform
  parameters:
    name: namespace
    chartPath: 'charts/namespace'
    repo: 'mycontainerrepo'
    stage: 'preprod'
    team: 'inv'
